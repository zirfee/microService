spring:
  application:
    name: server

server:
  port: 1113

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka,http://localhost:1112/eureka
  instance:
    hostname: server3
  server:
    enableSelfPreservation: false